#include "main.h"

/**
 * _isupper - check if a letter is upper
 * 0x: The number to be checked
 *
 * Return 1 for upper 0 for any else
 */
int _isupper(int x);
{
	if (x >= 65 && x <= 90)
	{
		return (i);
	}
	return (0);
}
